import javax.swing.*;

public class Bills extends JFrame{
    private JPanel Bill;

    public Bills(){
        setContentPane(Bill);
        setTitle("Menu");
        setSize(800,600);
        setLocationRelativeTo(null);
        setVisible(true);
        setDefaultCloseOperation(EXIT_ON_CLOSE);
    }
}
